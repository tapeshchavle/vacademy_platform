package vacademy.io.media_service.enums;

public enum QuestionResponseType {
    OPTION,
    INTEGER, // 0 - 9 integers
    ALL_INTEGER, // positive and negative integers
    NATURAL_NUMBER; // all positive integers
}
