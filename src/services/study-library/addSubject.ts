// import { useMutation, useQueryClient } from "@tanstack/react-query"

export const useAddSubject = () => {
    // const queryClient = useQueryClient();

    // return useMutation({
    //     mutationFn: async ({subject, packageSessionids}:{
    //         subject: subject
    //     }) =
    // })
    return 0;
};
