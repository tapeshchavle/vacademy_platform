apiVersion: v1
kind: Service
metadata:
  name: community-service
  namespace: default
spec:
  selector:
    app: community-service
  ports:
    - name: http
      port: 8073
      targetPort: 8073
  type: NodePort
