package vacademy.io.admin_core_service.features.admission.enums;

/**
 * Defines the two parallel application workflows.
 */
public enum AdmissionWorkflowType {
    APPLICATION, // Online Application (existing flow)
    ADMISSION // Manual Admission (new flow)
}
