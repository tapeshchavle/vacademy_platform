import { createLazyFileRoute } from "@tanstack/react-router"

export const Route = createLazyFileRoute("/instructor-copilot/")({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/instructor-copilot/"!</div>
}
